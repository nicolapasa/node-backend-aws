{
  "name": "mysql-table-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "dev": "node --env-file=.env --watch server.js",
    "check": "node --check server.js"
  },
  "engines": { "node": ">=20.12.0" },
  "dependencies": { "mysql2": "^3.0.0" }
}
